# apps/pwa-scanner

Not implemented yet. Vite + React installable PWA for barcode/QR scanning
(mobile + desktop), offline-first with an IndexedDB action queue.

Scaffolded starting spec §10 Phase 2, alongside the POS + Inventory modules
it depends on.
