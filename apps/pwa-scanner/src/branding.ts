/**
 * Store branding — one place to edit the display name shown in the app
 * header and used as the installed PWA's name/home-screen label. Editing
 * this one line updates both; no other code needs to change.
 *
 * index.html's <title> is plain static HTML (not processed at build time),
 * so it won't pick up a change here automatically — update it by hand too
 * if you change this.
 */
export const STORE_NAME = 'Hermes Scanner';
