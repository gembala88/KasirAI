# packages/shared-types

Not implemented yet. TypeScript types shared between apps/api, apps/dashboard,
and apps/pwa-scanner (e.g. API request/response DTOs, domain enums like
customer tier).

Populated as soon as a second app (dashboard or pwa-scanner) needs to share a
type with apps/api — see spec §10 Phase 2+.
