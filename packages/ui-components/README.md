# packages/ui-components

Not implemented yet. Shared design system component library used by both
apps/pwa-scanner and apps/dashboard (spec §9), so visual language stays
consistent and changes propagate to both.

Scaffolded once apps/dashboard or apps/pwa-scanner exists to consume it —
see spec §10 Phase 2+.
