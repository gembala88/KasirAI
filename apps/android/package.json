{
  "name": "@hermes/android",
  "version": "0.1.0",
  "private": true,
  "author": "KasirAI",
  "description": "KasirAI Android shell — Capacitor wrapper around the same PWA build used by apps/electron (see apps/pwa-scanner's build:electron script), with native camera permission for barcode scanning.",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/camera": "^6.1.2",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
